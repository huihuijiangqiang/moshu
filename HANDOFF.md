# 墨枢 · 开发交接包

AI 网文创作平台。本包含完整需求、界面设计与可运行的 Vue 3 前端脚手架。

## 先读这三个文件

| 文件 | 内容 | 怎么用 |
| --- | --- | --- |
| `需求文档与技术选型.dc.html` | MVP 范围、功能需求表（含验收标准）、分层记忆机制、技术选型、10 周里程碑、明确不做的清单 | 浏览器直接打开。**功能需求表是唯一的范围依据**，表外的东西不要做 |
| `界面设计.dc.html` | 十二屏完整界面设计 + 交给前端的实现约定 | 浏览器直接打开。每屏上方有一段设计意图说明，比像素更重要 |
| `写小说软件调研.dc.html` | 竞品调研与产品机会分析 | 背景材料。解释「为什么做一致性守卫和风格指纹」 |

> 这三个 HTML 依赖同目录的 `_ds/`（设计系统样式）和 `support.js`，整个文件夹一起保留才能正常打开。

## 前端代码

```
cd app
cp .env.example .env
npm i
npm run dev      # http://localhost:5173
npm test
```

默认 `VITE_USE_MOCK=true`，不接后端即可跑通全部十二个路由。详见 `app/README.md`。

## 产品定位（一句话）

以 AI 代写为主的网文生产平台。**一键成章是入场券，不是卖点**；差异化在长程一致性与作者风格保真。

- 目标用户：网文全职作者（日更 6-10k）、新人兼职作者、内容工作室
- 北极星指标：周留存作者中「AI 产出被采纳进正文的字数占比」
- 形态：Web 应用，桌面优先

## 四条不可妥协的技术约定

**1. 一章一文档。** 80 万字放进单个 ProseMirror 文档会卡死。章节列表不返回正文，按需拉取，切章卸载。

**2. 单次生成上下文硬上限 25k token。** 四层预算：稳定设定 6k（常驻 + prompt caching）／检索条目 5k／滚动摘要 4k／相邻原文 10k。任何功能不得突破——突破了成本模型就崩了。

**3. AI 输出落在 `aiDraft` 节点里，不进正文也不进撤销栈。** 流式增量全部 `addToHistory: false`，采纳是一次事务。这套行为有单测锁住（`app/src/editor/extensions/AiDraft.spec.ts`），改动前先跑测试。

**4. 设定引用存条目 id，不存文字。** 改名自动跟随，且服务端能精确知道本章引用了什么——一致性检查和上下文装配都依赖这份关系。

## 代码规范

- 组件名 PascalCase，目录名 kebab-case
- 单测用 Vitest，放在被测文件旁边（`*.spec.ts`）
- 样式：颜色、字号、间距一律用 `var(--*)` 取自 `app/src/styles/modernist.css`，不要硬编码十六进制
- 状态色只有两种：红（accent）= 需要处理或正在进行，灰阶 = 正常。不引入黄绿蓝

## 后续范围

- 工作室多人产线（P1，设计稿也未做）
- 协作编辑：**MVP 阶段不要引入 Yjs**，它会改写整个持久化模型

## 已落地的后端约定

1. 真实 API 已按 `app/src/api/mock/index.ts` 的方法签名接通，前端上层无需分叉
2. `/api/generate/chapter` 和 `/api/generate/inline` 走 SSE，候选草稿与正文隔离
3. 上下文装配（四层）在服务端完成，前端展示预算与上限
4. 摘要生成、一致性检查、风格抽取全部异步化，绝不阻塞写作

## 四条待决议（需要你拍）

1. 一致性守卫的召回率指标是否现实 —— 先做一周算法预研 spike，再对外承诺
2. 基础档模型选哪家 —— 实测中文网文语感，别只看榜单
3. 后端语言 Python 还是 Node —— 取决于团队现有人手
4. 是否 MVP 就上工作室席位版 —— 若已有明确付费客户，值得提前
