# Public browser configuration for the Cloudflare preview build.
# Preview uses the same HTTP API contract as production. Set this to true only
# for an explicitly isolated demo build.
VITE_USE_MOCK=false
VITE_API_BASE=/api
